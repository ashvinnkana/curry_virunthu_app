import 'package:flutter/material.dart';

class Choice {
  final String label;
  final String meat;
  final bool isVeg;
  final bool isAvailable;

  Choice(this.label, this.meat, this.isVeg, this.isAvailable);
}
